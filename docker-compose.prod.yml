version: '3.8'

services:
  server:
    image: ghcr.io/russelr00/hr-football-server:latest
    ports:
      - "3001:3001"
    restart: unless-stopped
    environment:
      - PORT=3001
      - PUBLIC_URL=https://ip-172-26-4-83.taild77fe0.ts.net
      - HOST_PASSWORD=deep2026

  client:
    image: ghcr.io/russelr00/hr-football-client:latest
    ports:
      - "80:80"
    restart: unless-stopped
    depends_on:
      - server
